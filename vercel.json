{
    "version": 2,
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm turbo run build --filter=front-end...",
    "outputDirectory": "apps/web/.next",
    "installCommand": "npm i -g pnpm",
    "framework": "nextjs"
}