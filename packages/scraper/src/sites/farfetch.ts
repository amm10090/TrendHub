// Farfetch scraper placeholder
export default async function scrapeFarfetch(startUrls?: string | string[]) {
  console.log("Scraping Farfetch (placeholder)", startUrls);
  return Promise.resolve([]);
}
