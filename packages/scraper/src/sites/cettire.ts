// Cettire scraper placeholder
export default async function scrapeCettire(startUrls?: string | string[]) {
  console.log("Scraping Cettire (placeholder)", startUrls);
  return Promise.resolve([]);
}
