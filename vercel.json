{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "corepack prepare pnpm@10.8.1 --activate && pnpm install"
      }
    }
  ],
  "buildCommand": "corepack prepare pnpm@10.8.1 --activate && pnpm run build"
}
